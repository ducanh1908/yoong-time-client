import React from 'react';
import PropTypes from 'prop-types';
import TopBar from '../../components/TopBar/TopBar';

Home.propTypes = {
    
};

function Home(props) {
    return (
        <div>
            <TopBar />
        </div>
    );
}

export default Home;